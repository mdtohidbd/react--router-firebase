import React from 'react';




const AuthContext = React.createContext(null);


const AuthProvider = () => {
    return (
        <div>
            
        </div>
    );
};

export default AuthProvider;